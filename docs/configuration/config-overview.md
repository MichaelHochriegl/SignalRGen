# Configuration

`SignalRGen` allows for a multitude of configuration settings.
These can be grouped into two categories:
1. global
2. per-Hub

These two allow for different configurations taking place, so it's not like you can define the same settings globally
and per-Hub.